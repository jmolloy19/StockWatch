syntax = "proto3";

package sw;

message TickerList {
    repeated Ticker tickers = 1;
}

message Ticker {
    string currency = 1;
    string description = 2;
    string display_symbol = 3;
    string symbol = 4;
    string type = 5;
}
